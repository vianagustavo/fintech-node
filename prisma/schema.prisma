generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model People {
  id String @id @default(uuid())
  name String
  document String
  password String
  createdAt DateTime
  updatedAt DateTime

  @@map("people")
}
